import { StubPage } from "@/components/StubPage";

export default function DelaStub() {
  return (
    <StubPage
      breadcrumb="Dela"
      title="Dela passet med en vän"
      body="Skicka en länk via Messenger, Whatsapp eller sms. Funktionen bygger vi nu."
      backHref="/boka"
      backLabel="Tillbaka till pass"
    />
  );
}
